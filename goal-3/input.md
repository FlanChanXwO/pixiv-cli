使用 goal-mode skill 管理 Goal-3，但保持 Goal-3 为单一完整 Goal。先审视并采纳《Goal-3 vNext 计划修改建议报告》，再修订 plan、tasks、risk、admission、migration verification、CLI migration matrix 与相关计划资料。

重点：Phase A 从 API discovery 改为 Contract Freeze；复用现有 sdk.Cursor、sdk/pixiv continuation、internal/shared/pagination、internal/shared/traversal 和 pixiv.ParseURL；T12 先冻结 Public SDK Compatibility Decision；CLI 按 owner 拆成独立 task；CLI migration matrix 冻结后再进入 public surface。

完成计划资料修订与一致性审查后汇报，不直接开始代码实现。
